# Template for .a.env — arm A of the proposer-policy A/B experiment.
# Copy to .a.env (gitignored) and fill in any per-arm overrides.
#
# Arm A is the CONTROL: the default find_considerations prompt.

FIND_CONSIDERATIONS_VARIANT=default

# Add any other per-arm overrides below. Anthropic / Supabase creds are
# inherited from the base .env that ab_branch.sh copies into each worktree.
# ANTHROPIC_API_KEY=<your-key>
