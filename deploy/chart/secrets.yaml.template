# Template for secrets.enc.yaml
# Fill in values and encrypt with:
#   sops encrypt --gcp-kms projects/varuna-400921/locations/global/keyRings/sops/cryptoKeys/sops-key \
#     secrets.yaml.template > secrets.enc.yaml

secrets:
  api:
    SUPABASE_PROD_KEY: ""     # service_role key from Supabase dashboard → Settings → API
    SUPABASE_JWT_SECRET: ""   # JWT secret from Supabase dashboard → Settings → API → JWT Settings
    ANTHROPIC_API_KEY: ""
    VOYAGE_AI_API_KEY: ""
    JINA_API_KEY: ""
  frontend:
    INVITE_PASSWORD: ""       # shared passphrase gating the landing page
    INVITE_SECRET: ""         # ≥32-char random string for HMAC-signing invite cookies
