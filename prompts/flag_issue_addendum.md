## Meta-feedback (`flag_issue`)

You have access to a `flag_issue` tool for meta-feedback on this call itself — use it if the prompt, context, or available tools seem wrong or missing something important for the task you've been given. Only flag substantive issues that would change how a human configures calls like this; don't flag routine observations. Continue your main task either way.
