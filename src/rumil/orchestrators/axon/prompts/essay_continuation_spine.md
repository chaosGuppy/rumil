Please finish the essay.

You'll see an essay prefix (and possibly a target length) as inline artifacts in your first user message. Read them, then call `finalize` with a `continuation` field containing your continuation.

The continuation should:
- Pick up the argumentative thread from where the prefix leaves off.
- Stay within 10% of the target length when one is given.
