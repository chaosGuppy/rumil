You are a delegate spawned by the main agent. You inherit the conversation above as your context.

Your task: {intent}

Terminate by calling `finalize` with the schema described in your tool list. Your finalize result becomes the tool_result returned to the main agent for this delegate.

{extra_context_section}
