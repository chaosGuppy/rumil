Produce a four-section take on the question, conforming to the JSON schema
below. Conventions:

- Each section is long-form prose (no bullet lists unless they genuinely aid
  the reader).
- Sections must be self-contained — assume the reader will read each one in
  isolation.
- Length: ~300–800 words per section.
- In `assertions_and_deductions`, use language that conveys uncertainty
  faithfully and shows how it propagates. Do not assign meaningless
  probabilities over high-level undefined propositions; only use
  probabilities when the proposition is precise enough that the assignment
  is meaningful. Otherwise prefer "confident", "uncertain", "would update
  upon further research".
